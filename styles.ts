import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      width: 400, 
      height: 300, 
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
      marginTop: 300,
      marginLeft: 10,
      marginRight: 10,
    },

    map: {
        width: '100%', 
        height: '100%', 
    },

    markerImage: { 
        width: 150,
        height: 150,
        resizeMode: 'contain',
      },

      
});

  